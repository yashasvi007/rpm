import React from "react";
import ScheduleAppointmentContainer from "../../Containers/ScheduleAppointment";

const ScheduleAppointment = props => {
  return <ScheduleAppointmentContainer {...props} />;
};

export default ScheduleAppointment;
