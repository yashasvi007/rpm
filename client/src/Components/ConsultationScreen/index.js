import React from "react";
import ConsultationScreenContainer from "../../Containers/ConsultationScreen";

const ConsultationScreen = props => {
  return <ConsultationScreenContainer {...props} />;
};

export default ConsultationScreen;
